// Otra forma de declarar clases, esta vez de forma anónima.

let Mesa = class {
    constructor(alto, ancho, largo) {
        this.alto = alto;
        this.ancho = ancho;
        this.largo = largo;
    }
}